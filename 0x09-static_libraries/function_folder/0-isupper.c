/**
 * _isupper - Changes lowercase letters to uppercase
 * @a: The character being changed
 * Return: integer
 */

int _isupper(char a)
{
	if (a >= 'A' && a <= 'Z')
	{
		return (1);
	}
	return (0);
}
